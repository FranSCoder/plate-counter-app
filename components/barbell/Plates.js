import { Text } from 'react-native'

const RenderPlateImages = async () => {
  // const plateImages = []
  // for (const color in loadedPlates) {
  //   const count = loadedPlates[color]
  //   console.log(`../../assets/images/${'red'}.png`)
  //   const image = Asset.fromModule(require(`../../assets/images/red.png`))
  //   await image.downloadAsync()
  //   if (count > 0) {
  //     for (let i = 0; i < count; i++) {
  //       plateImages.push(
  //         <Image
  //           key={`${color}-${i}`}
  //           source={image}
  //           style={styles[color]}
  //         />
  //       )
  //     }
  //   }
  // }
  return <Text>Yo</Text>
}

export default RenderPlateImages
